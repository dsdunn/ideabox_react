 import React, { Component } from 'react';

export default class Container {
  render() {
    return (
        <div className="cardContainer">
          {
          }
        )}
        </div>
      )
  }
}